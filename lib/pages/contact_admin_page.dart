import 'package:flutter/material.dart';

class ContactAdminPage extends StatelessWidget {
  const ContactAdminPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('This is the Contact Admin Page')),
    );
  }
}
