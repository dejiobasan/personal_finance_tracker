import 'package:flutter/material.dart';

class YourTransactionsPage extends StatelessWidget {
  const YourTransactionsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('This is the Your Transactions Page')),
    );
  }
}
